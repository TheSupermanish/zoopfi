// Empty stub used to alias dev-only Node modules (e.g. why-is-node-running)
// that get pulled in transitively by pino/thread-stream via WalletConnect but
// must never be bundled for the browser.
module.exports = {};
